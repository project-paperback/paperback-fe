import { NavLink } from "react-router-dom";

const NavigationLinks = () => {
  return (
    <div>
      <NavLink>Bookstore</NavLink>
      <NavLink>Sign Up</NavLink>
      <NavLink>Sign In</NavLink>
      <NavLink>Contact</NavLink>
    </div>
  );
};

export default NavigationLinks;
